# Sprint: 2026-06-06

## Production Bug Fixes

### Issues Fixed
- **Issue 2 (HIGH)**: Removed `requireAuth` from `POST /api/auth/register` — registration now public
- **Issue 3 (CRITICAL)**: Updated auth endpoints documentation — login is client-side Firebase Auth (no backend endpoint)
- **Issue 4 (MEDIUM)**: Changed chatbot from `requireAuth` to `optionalAuth` — public access with rate limiting
- **Issue 6 (MEDIUM)**: Added security headers (CSP, XFO, XCTO) to `next.config.ts` — build verified

### Issues Skipped (Deferred)
- **Issue 5 (MEDIUM)**: Artifacts gallery SSR refactor — shows "0 artifacts" in SSR payload. Requires splitting client component into server wrapper + client sub-component for proper SSR data fetching. Deferred to future.
- **Issue 1 (HIGH)**: Google Maps API key not set in production — requires manual Vercel env var configuration

### Manual Actions Required
- Add `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` to Vercel project env vars
- Add `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID` to Vercel project env vars

### Files Modified
| File | Change |
|------|--------|
| `src/routes/auth.routes.js` | Removed `requireAuth` from register route |
| `src/routes/ai.routes.js` | Changed chatbot from `requireAuth` to `optionalAuth` |
| `docs/api/auth-endpoints.md` | Corrected endpoint docs — login is client-side |
| `next.config.ts` | Added security headers (CSP, XFO, XCTO, etc.) |
| `PROJECT_STATE.md` | Added session summary |
| `docs/sprints/2026-06-06.md` | This file |

### Build Verification
- Backend: `node -c` syntax checks passed on modified files
- Frontend: `npm run build` — zero errors, all 9 routes generated
